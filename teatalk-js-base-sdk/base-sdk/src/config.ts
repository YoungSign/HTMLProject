export default class config {
    static readonly DEFAULT_KEEP_ALIVE: number = 60;
    static readonly DEFAULT_CLEAN_SESSION: boolean = false;
    static readonly DEFAULT_PACKET_TIMEOUT: number = 15;
}